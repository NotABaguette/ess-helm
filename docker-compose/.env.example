# Copy this file to .env and adjust values
SERVER_NAME=example.com
SYNAPSE_HOST=matrix.example.com
MAS_HOST=account.example.com
ELEMENT_WEB_HOST=chat.example.com
ELEMENT_ADMIN_HOST=admin.example.com
HOOKSHOT_HOST=hookshot.example.com
MRTC_HOST=mrtc.example.com

TZ=UTC

POSTGRES_PASSWORD=change-me-postgres
SYNAPSE_DB_PASSWORD=change-me-synapse-db
MAS_DB_PASSWORD=change-me-mas-db

SYNAPSE_REGISTRATION_SHARED_SECRET=change-me-registration-secret
SYNAPSE_MACAROON_SECRET_KEY=change-me-macaroon-secret
MAS_SECRETS_ENCRYPTION=change-me-mas-encryption-secret
MAS_MATRIX_SECRET=change-me-mas-matrix-secret

# Generate with: openssl rand -base64 48
HOOKSHOT_PASSKEY=change-me-hookshot-passkey

# Public URL where nginx is reachable
PUBLIC_BASE_URL=https://${SYNAPSE_HOST}
